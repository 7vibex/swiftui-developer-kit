# Claude Prompt Examples

```text
Use the swiftui-project-router skill. audit this SwiftUI app and choose the narrowest specialist workflows.
```

```text
Use the swiftui-design-system-auditor skill. Review whether this iPad app feels native. Focus on layout hierarchy, spacing, typography, SF Symbols, toolbar placement, sidebar behavior, empty states, keyboard, pointer, and Apple Pencil workflows.
```

```text
Use the swiftui-ui-patterns skill. Refactor this editor screen's state ownership, sheets, async loading, previews, and subview structure.
```

```text
Use the canvas-engine-auditor skill. Audit my iPad Canvas for PencilKit drawing bugs, coordinate drift after zoom/pan, highlighter opacity, layer save/reopen, gesture conflicts, persistence loss, undo/redo, performance, and missing regression tests.
```

```text
Use the swiftui-diagnostics-builder skill. Plan a privacy-safe Report Issue workflow with Logger categories, breadcrumbs, app-state snapshots, MetricKit, and AI-readable export JSON.
```

```text
Use the liquid-glass-placement-auditor skill. Ask before screenshots, then review where Liquid Glass belongs and where stable contrast matters more.
```

```text
Use the swiftui-localization-auditor skill. Audit String Catalogs, language switching, pluralization, locale formatting, RTL, and localized accessibility strings.
```

```text
Use the apple-app-security-privacy-auditor skill. Audit data flows, Keychain, auth, networking, deep links, storage, permissions, entitlements, and privacy disclosures without exposing secrets.
```
