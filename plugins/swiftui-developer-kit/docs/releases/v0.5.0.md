# v0.5.0 - Scanner Release

Status: Unreleased planning; no `v0.5.0` tag or publication exists.

## Evidence

- SwiftSyntax package and structured finding model.
- Versioned scanner JSON schema.
- Baseline and inline suppression support.
- Positive and negative rule fixtures.

## Acceptance

- Structured scanner tests pass.
- At least ten rule identifiers are covered.
- JSON output validates against the checked-in schema.
- Shell scanner remains available as a dependency-light fallback.
